This directory contains machine specific files for compiling POV-Ray on Vax.
It contains:

MACHINE\VAX\
   vaxconf.h    - Vax configuration header -- rename to config.h
   vax.c        - system specific source for Vax
   xwind.c      - xwindows display code
   vaxbuild.com - ordinary compile and link DCL script.
   xvaxbld.com  - XWindows compile and link DCL script.
   povray.opt   - link options file for X windows version.
   vax.cat      - list of files in this directory